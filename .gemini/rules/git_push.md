# Git Push Branch Requirement

All git commits and git pushes for this codebase MUST target the `test/fooocus-prompts` branch.
DO NOT push to `main` until explicitly instructed by the user after a feature is verified working.

Command to use:
`git push origin test/fooocus-prompts`
